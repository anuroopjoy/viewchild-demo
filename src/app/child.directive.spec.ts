import { ChildDirective } from './child.directive';

describe('ChildDirective', () => {
  it('should create an instance', () => {
    const directive = new ChildDirective();
    expect(directive).toBeTruthy();
  });
});
